import { Register as RegisterForm } from '../components';

export default function Register() {
    return (
        <main className="min-h-full bg-slate-100 dark:bg-slate-800 dark:text-white flex items-center justify-center relative">
            <RegisterForm />
        </main>
    );
}